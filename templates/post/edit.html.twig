{% extends 'base.html.twig' %}

{% block title %}Edit Post{% endblock %}

{% block body %}
    <div class="container">
        <h1>Edit Post</h1>

        <div class="card mb-4">
            <div class="card-body">
                {{ include('post/_form.html.twig', {'form': form, 'button_label': 'Save'}) }}
            </div>
        </div>

        <a href="{{ path('app_post_index') }}">Back to list</a>
    </div>
{% endblock %}